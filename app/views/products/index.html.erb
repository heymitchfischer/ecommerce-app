<h1>All Products:</h1>

<% @products.each do |product| %>
  <div class="product">
    <h3><%= link_to("#{product.name}", "/products/#{product.id}") %></h3>
    <p><%= product.price_format %></p>
  </div>
<% end %>